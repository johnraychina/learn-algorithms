package 每日一题;

/**
 * 47. 全排列 II
 *
 * 给定一个可包含重复数字的序列 nums ，按任意顺序 返回所有不重复的全排列。
 */
public class 全排列47 {

    // 两种方法来解决重复排列问题：
    // 1. 生成排列时，先判断是否已经存在相同的排列，需要加多一轮查找
    // 2. 排列前，先将nums排序，然后排列时，记录已填入状态，并且只填入第一个
}
